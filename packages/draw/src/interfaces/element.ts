export enum StrokeStyle {
    solid = 'solid',
    dashed = 'dashed',
    dotted = 'dotted'
}

export enum MemorizeKey {
    basicShape = 'basicShape',
    flowchart = 'flowchart',
    text = 'text',
    line = 'line'
}
