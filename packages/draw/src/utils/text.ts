import { PlaitElement } from '@plait/core';
import { PlaitCommonGeometry, PlaitGeometry } from '../interfaces';
import { isMultipleTextGeometry } from './multi-text-geometry';

// export const getTextProperty = <T extends PlaitCommonGeometry = PlaitElement>(element: T) => {
//     if (isMultipleTextGeometry(element as  PlaitCommonGeometry)) {
        
//     }

// };
